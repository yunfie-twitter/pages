---
import { getYoutubeHref, getYoutubeThumbnail, tracks } from "../data/tracks";
---

<section id="music" class="section">
  <h2>MUSIC</h2>

  <p>ゆんふぃの公式楽曲リストです。</p>

  {
    tracks.length > 0 ? (
      <div class="music-grid">
        {tracks.map((track) => (
          <a
            class="music-card"
            href={getYoutubeHref(track.youtubeId)}
            target="_blank"
            rel="noopener noreferrer"
            aria-label={`${track.title} を開く`}
          >
            <div class="music-thumb">
              <img
                src={track.thumbnail ?? getYoutubeThumbnail(track.youtubeId)}
                alt={track.title}
                loading="lazy"
                width="480"
                height="270"
              />
            </div>
            <div class="music-info">
              <small>{track.date}</small>
              <span>{track.title}</span>
              {track.description && <p>{track.description}</p>}
            </div>
          </a>
        ))}
      </div>
    ) : (
      <p class="music-empty">楽曲は準備中です。</p>
    )
  }
</section>
